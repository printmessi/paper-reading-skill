---
name: paper-reading-workflow
version: 0.1.0
description: 面向工科研究生的单篇论文阅读与知识沉淀工作流。以 Zotero 为文献入口、Obsidian 为知识库、Codex 为阅读与串联中枢，支持快速筛选、深度精读、知识库整理、证据定位、跨会话状态和安全的 Zotero/Obsidian 更新提案。
---

# Paper Reading Workflow

## 目标

把一篇论文从“待读文献”推进到“可复用知识资产”，同时避免 AI 替用户思考、伪造证据或污染知识库。

本 Skill 第一版只处理**单篇论文**，不做批量筛选，不自动推荐下一篇论文。

## 核心角色分工

- **Zotero**：文献资产管理。Collection 管研究主题；Tag 管阅读状态；原始 PDF 保持只读。
- **Obsidian**：正式知识库。只保存用户确认过的最终论文主笔记、知识节点和主题 MOC。
- **Codex**：阅读、科研判断、证据整理、知识串联与提案。
- **脚本**：仅处理文件、路径、编号、状态、Zotero 元数据读取、Vault 扫描等确定性工作；不得替代科研判断。

## 首次使用

首次发现当前项目尚未初始化，或用户说“初始化论文阅读工作区”“使用帮助”，先读取：

1. `modules/10-onboarding.md`
2. `docs/SETUP_WINDOWS.md`
3. `docs/ZOTERO_SETUP.md`
4. `docs/OBSIDIAN_SETUP.md`
5. `docs/WORKFLOW.md`

不要自动安装软件、修改系统环境变量、修改 Zotero/Obsidian 设置。只检查、解释并生成草稿；真实配置改动由用户执行。

## 中文自然语言命令

识别以下意图，不要求用户精确匹配字符串：

- `初始化论文阅读工作区`
- `创建研究主题`
- `筛选论文`
- `精读论文`
- `整理知识库`
- `完成论文笔记`
- `整理 Zotero`
- `更新 Zotero 状态`
- `检查阅读进度`
- `检查当前论文流程`
- `使用帮助`

总入口只根据用户命令执行当前阶段。可以提醒下一阶段，但**不得擅自越级执行**。

## 强制前置条件：主论文必须由用户提供

当前主论文必须存在于以下任一位置：

1. Zotero 条目已附 PDF；或
2. 用户直接提供 PDF/正文文件。

如果只有标题、DOI、arXiv、网页链接或摘要而没有可读取全文：

- 停止论文内容分析；
- 提醒用户上传 PDF 或把 PDF 加入 Zotero；
- 不自行上网搜索主论文全文；
- 不猜测章节、页码、公式、图表、实验数据或结论。

在主论文 PDF 已存在后，**深度精读**阶段允许为横向对比检索少量外部文献；外部信息必须标注来源。

## 三阶段工作流

### 阶段 1：快速筛选

触发：`筛选论文`

读取 `modules/01-screening.md`。

输出只在对话中，不生成正式 Markdown 笔记。目标阅读时长约 5–10 分钟，通常约 1500–2500 中文字，按复杂度调整。

推荐结论四档：

1. **强烈推荐精读**
2. **推荐精读**
3. **建议重点阅读部分章节**
4. **不建议继续阅读（No）**

必须同时给出：

- 论文核心问题、核心论点、方法思路、主要结论；
- 论文类型及混合标签；
- 理论与分析方法；
- 创新性、方法学习价值、实验扎实程度、理论/分析方法价值、可复现性、选题启发；
- 阅读重点及**章节 + 页码**；
- “如果只花 20 分钟，应该读哪里”；
- 判断依据与证据状态。

快速筛选默认只依据主论文，不因期刊名气、引用量或作者名气先入为主。

若结论为 No：建议 Zotero 状态 `状态/No`，并在工作区保存简短筛选记录，不进入 Obsidian。

### 阶段 2：深度精读

触发：`精读论文`

读取：

- `modules/02-deep-reading.md`
- `modules/06-paper-types.md`
- `modules/07-evidence-and-citations.md`

输出到工作区 `.md`，这是**中间精读稿，不是最终 Obsidian 主笔记**。

结构要求：

- 前半段偏教学：概念、前置知识、方法、关键公式、关键图表；
- 后半段偏科研分析：创新、实验设计、局限、可复现性、横向对比、选题启发；
- 核心公式：保留原式、LaTeX 重写、逐变量解释、说明作用与前提，标注 Section / PDF 页 / Paper 页 / Eq.；
- 关键图表：解释“图表证明什么、怎么看、是否支持结论、可借鉴什么”；
- 默认加入少量横向对比，通常 2–5 篇，优先原始论文/官方来源；
- 简短复现清单；
- Mermaid 仅在确实帮助理解时加入；
- 最后加入主动回忆（active recall）与研究式问题。

特别长的论文采用“分段分析 → 结构化缓存 → 最终汇总”。

完成后建议 Zotero 状态仍为 `状态/精读`；当深度精读已完成、等待知识库整理时建议改为 `状态/整理`。

### 阶段 3：知识库整理

触发：`整理知识库` 或 `完成论文笔记`

读取：

- `modules/03-knowledge-integration.md`
- `modules/05-obsidian.md`
- `modules/07-evidence-and-citations.md`

仅搜索该论文状态文件绑定的**指定研究主题/文件夹**，不得默认扫描整个 Vault。

流程：

1. 读取深度精读稿、用户在 Obsidian 主题目录中的相关知识点、可选 Zotero 注释；
2. 先自动串联已有材料，形成半成品逻辑链；
3. 对真正缺少用户理解的关键处，少量逐个提问；
4. AI 绝不代写 `【我的理解】`；没有用户输入时写 `暂无 / 待补充`；
5. 生成 `knowledge-update-proposal.md`：列出拟新建/拟修改文件、双向链接、MOC 更新、理由与 diff 摘要；
6. 在用户明确确认前，不写入 Obsidian；
7. 如用户确认，再生成/更新最终论文主笔记与知识节点；
8. 最终主笔记按“研究背景 → 核心问题 → 理论基础 → 假设/出发点 → 方法 → 证据 → 结论 → 局限 → 知识关联”组织，不按原论文目录机械复述；
9. 每个核心主题下并列 `【作者观点】`、`【我的理解】`、`【AI 分析】`；
10. 写入完成后建议 Zotero 状态 `状态/已读`。

YAML/Properties 是可选增强。知识库整理前必须说明用途并询问用户是否启用，未确认不得自动加元数据块。

## 论文类型自动分类

第一步先分类，可多标签混合：

- 理论 / 数学建模类
- 算法 / AI / 信号处理类
- 实验 / 测量类
- 硬件 / 系统实现类
- 材料 / 器件类
- 控制 / 通信 / 工程系统类
- 综述 / Review / Survey 类

读取 `modules/06-paper-types.md` 选择重点。综述类必须使用独立模板，以 taxonomy、技术路线演化、方法优缺点、常用数据集/指标、研究空白与未来方向为主，不逐段复述。

## 证据与可靠性：最高优先级硬规则

始终读取 `modules/07-evidence-and-citations.md`。

统一标记：

- `【原文明确】`：论文直接给出的事实、数据、作者结论。
- `【根据原文分析】`：对原文进行的有限推断。
- `【外部证据】`：来自横向对比论文或官方来源。
- `【我的理解】`：仅限用户自己的输入。
- `【AI 分析】`：AI 的分析、比较、建议。
- `【待核验】`：证据不足或解析不可靠。
- `【暂无】`：论文未提供或当前材料没有。

严禁：

- 虚构页码、公式编号、图表编号、实验数值、数据集规模、作者结论、引用关系；
- 把 AI 推断写成作者观点；
- 把没有用户输入的内容写成“我的理解”；
- 在 PDF 无法可靠解析时装作已核验。

无法确认就明确写：`暂无` / `未提供` / `无法确认` / `待核验`。

## 定位格式

优先使用双页码：

`Section 3.2 · PDF p.8 · Paper p.1423 · Eq. (6)`

图表：

`Section 4.1 · PDF p.10 · Paper p.1425 · Figure 4`

若论文没有印刷页码，只保留 PDF 页码。若扫描版/解析质量不足，明确标记，不伪造定位。

引用策略：中文转述为主；对关键定义、创新点、局限性可保留短原文用于核验。正文简洁引用，文末列完整来源。

## Zotero 安全规则

读取 `modules/04-zotero.md`。

- 默认入口为 Zotero。
- 原始 PDF 只读；分析前复制到工作区临时目录。
- Better BibTeX 提供稳定 citekey，作为 Zotero ↔ Obsidian ↔ Codex 的核心文献标识。
- Collection 管研究主题；Tag 管状态。
- 状态：`状态/待读` → `状态/精读` → `状态/整理` → `状态/已读`；不值得继续读则 `状态/No`。
- 可以提出标签/Collection/状态整理建议，但**只有用户明确下达“更新 Zotero …”并确认后才修改**。
- **绝不直接写 `zotero.sqlite`**。
- 优先 Zotero Local API / Better BibTeX 本地接口；写入必须走支持授权的安全接口。

## Obsidian 安全规则

- 工作区与 Vault 严格分离。
- 工作区保存 PDF 副本、深度精读、中间缓存、状态、提案。
- Obsidian 只保存用户确认后的正式知识。
- 只检索用户指定研究主题目录。
- 写入前必须生成提案/diff 摘要并等待确认。
- 节点创建遵循“有复用价值才独立建节点”，避免碎片化。
- 可以维护主题 MOC，但仍需先提案。
- 同一篇论文以 citekey 识别，持续迭代，不新建 v2/final-final；更新前先提案，并保留更新记录。

## 文件命名与编号

每个研究主题独立编号：`001`、`002`……

最终论文主笔记文件名：

`NNN-Shortened English Title.md`

例如：`007-Hyperspectral Classification with Transformers.md`

citekey 放在内部元数据或笔记正文中，不强行塞进文件名。

## 阅读进度

每篇论文保存一个工作区状态文件，至少记录：

- citekey / Zotero item key
- 研究主题
- 当前状态
- 快速筛选完成情况
- 深度精读完成情况
- Obsidian 整理完成情况
- Zotero 建议状态
- 最后更新时间

用户说“检查阅读进度”时，读取状态并只汇报已完成、待完成和建议下一步。

## 质量自检

用户说“检查当前论文流程”，或任何正式写入前，读取 `modules/09-quality-check.md`。

至少检查：

- 主论文全文是否实际可读；
- 论文类型是否已识别；
- 关键判断是否带可追溯证据；
- 是否出现无法核验却写成事实的内容；
- 深度精读必要章节是否齐全；
- citekey 是否唯一；
- 状态文件是否更新；
- Obsidian 写入是否有提案与用户确认；
- Zotero 写入是否有用户明确指令与授权；
- Git/diff 是否可用于回退。

## 隐私与联网

默认：**最小权限 + 本地优先 + 按需联网**。

- 不主动扫描整个 Zotero 库或整个 Obsidian Vault；
- 不把私人批注或整个知识库发给外部服务；
- 主论文分析基于用户提供 PDF；
- 只有深度精读横向对比等任务才按需联网；
- 日志只保存必要状态、路径、citekey，不保存无必要的全文副本。

## 下一阶段提醒

允许提醒，不允许自动执行。例如：

- 筛选后：`建议下一步：精读论文`
- 精读后：`建议下一步：将 Zotero 状态改为 状态/整理，并在 Obsidian 补充你的知识点`
- 用户知识点已存在：`建议下一步：整理知识库`
- 最终主笔记确认写入后：`建议下一步：更新 Zotero 状态为 状态/已读`

## 第一版明确不做

- 多篇论文批量筛选；
- 自动推荐下一篇阅读；
- 在没有主论文 PDF 时自行搜全文；
- 自动创建大量知识节点；
- 自动安装软件/插件；
- 无确认写入 Zotero 或 Obsidian；
- 直接操作 Zotero SQLite 数据库。
